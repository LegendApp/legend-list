// Deprecated: web no longer uses Animated polyfill. Kept for compatibility if some code still imports it.
export const Animated = {} as any;
export const AnimatedValue = undefined as any;
export default Animated;
